{
  "name": "base-lint-action",
  "version": "1.1.2",
  "description": "GitHub Action wrapper for the base-lint CLI, which lints your repo against Web Baseline policies.",
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "dist",
    "action.yml",
    "README.md"
  ],
  "imports": {
    "vitest": "../../tests/mocks/vitest.js"
  },
  "scripts": {
    "build": "node ../../scripts/build-action.mjs",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  }
}
