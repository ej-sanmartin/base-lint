{
  "name": "base-lint-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for the base-lint CLI and GitHub Action",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/cli && npm run build -w packages/action",
    "test": "npm run test:unit",
    "test:unit": "node scripts/run-unit-tests.mjs"
  },
  "imports": {
    "vitest": "./tests/mocks/vitest.js"
  }
}
